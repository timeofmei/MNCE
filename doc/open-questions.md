# 待讨论事项

## 产品与交互

- 三种学习模式的最终操作流程和页面布局
- 逐句播放前后静音的默认值、调节范围和设置位置
- 影子跟读录音、重录和原音/录音对比的按钮流程
- 听写提交、显示答案和进入下一句的流程
- 全文背诵录音、等待识别、结果对照和回放的流程
- 字符级差异的颜色和插入、删除、替换表现
- 英语和日语文本比较的完整规范化规则

## 分句

- AI 分句请求与结构化响应协议
- AI 可添加哪些标点，以及如何保证 Ground Truth 不被改写
- AI 句界映射到 Whisper 时间轴的策略
- 本地英语和日语分句规则
- 超长转写文本的分块方式
- DeepSeek 预设使用滚动模型别名还是固定模型版本

## 模型与性能

- 用真实学习材料确认英语 `base.en-q8_0` 和日语 `small-q8_0`
- 确定模型准确率、句界质量、内存占用和识别速度的验收标准
- 决定是否在 MVP 内提供同一语言的多个模型尺寸
- Apple Silicon 是否默认启用 Metal

## 工程与发行

- 锁定 FFmpeg 的支持格式和构建配置
- 确定内容哈希算法和大文件哈希体验
- 确定播放器和麦克风录音的具体实现
- 确定跨平台 API Key 安全存储方案
- 确定 `.deb` 和 `.rpm` 的依赖策略及发行时仍受维护的测试版本
- Windows 与 macOS 代码签名
- MVP 是否包含应用自动更新

## 项目标识

- 正式反向域名、应用 ID、包名和数据库名称
- 图标和视觉风格
- 版本号规则和首次发行版本号
